import { CollectionPage } from '@/components/collection-page';

export default function ProjectsPage() {
  return (
    <CollectionPage
      collection="projects"
      title="Projects"
      description="Open-source systems and operational experiments where hypotheses are translated into working artifacts."
    />
  );
}
